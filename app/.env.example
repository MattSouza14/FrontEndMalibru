
# URL do backend (proxy Vite em dev: /api → este host)
API_PROXY_TARGET=http://localhost:8080

# Autenticação via cookies HttpOnly:
# - malibru_access_token (15 min)
# - malibru_refresh_token (7 dias)
# Todas as requisições autenticadas usam credentials: 'include'.
# Backend: CORS com Allow-Credentials + origin específico (ex.: http://localhost:5173).
