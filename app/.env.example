
# URL do backend (proxy Vite em dev: /api → este host)
API_PROXY_TARGET=http://localhost:8080

# Autenticação: o backend deve emitir JWT via Set-Cookie (HttpOnly, Secure em prod, SameSite)
# e aceitar o cookie nas rotas autenticadas. CORS com Allow-Credentials + origin específico.
