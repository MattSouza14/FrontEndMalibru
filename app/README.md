# Pacote do portal

A documentação principal está no [README da raiz](../README.md).

Nesta pasta: npm ci, npm run dev, npm run check e npm run build.

As versões de dependências estão em package-lock.json. Não usar npm install na CI, nem versionar node_modules, dist ou arquivos .env com valores locais.
